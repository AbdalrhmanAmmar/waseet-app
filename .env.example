# Public configuration; never put secrets in EXPO_PUBLIC_* values.
EXPO_PUBLIC_API_URL=https://smart-broker-bcd9bzfvagd5f8dx.westus-01.azurewebsites.net/api/
# Optional endpoint accepting { token, platform }. Enable after confirming the backend contract.
# Native tokens are FCM on Android and APNs on iOS.
# EXPO_PUBLIC_PUSH_TOKEN_PATH=devices/push-token
# EXPO_PUBLIC_EAS_PROJECT_ID=
# Local path or EAS file variable for the Android Firebase application (com.selling).
# GOOGLE_SERVICES_JSON=
